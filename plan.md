# 基于执行反馈的强化学习代码生成研究

## 一、研究概述

本课题围绕"基于执行反馈的强化学习（RLEF）"核心思想，研究如何通过强化学习方法优化大语言模型的代码生成能力。

---

## 二、研究内容

### 2.1 面向代码生成的强化学习环境（RL Environment）构建

基于实习期间开发的 Docker 代码沙箱，设计并实现符合 OpenAI Gym 接口标准的强化学习交互环境：

- **状态空间设计**：解析代码生成的上下文（Prompt + Context）作为状态输入
- **沙箱接口封装**：利用 FastAPI 网关，实现代码的自动化提交、编译、执行与测试
- **安全性隔离**：确保在强化学习大规模采样过程中，恶意代码或死循环不会导致宿主机崩溃，实现资源的细粒度限制

### 2.2 代码生成任务的数学建模与奖励函数设计

将代码生成过程形式化为离散动作空间下的马尔可夫决策过程（MDP）：

- **MDP 建模**：定义状态空间 $S$、动作空间 $A$（词表 token）、策略 $\pi_\theta$ 和状态转移概率
- **奖励塑形（Reward Shaping）**：针对代码任务"稀疏奖励"问题，设计分层奖励函数 $R(s,a)$：
  - $R_{compile}$：编译通过奖励
  - $R_{test}$：通过测试用例的比例奖励
  - $R_{efficiency}$：基于运行时间和内存占用的性能奖励

### 2.3 基于 PPO 的在线策略优化算法实现

研究并实现近端策略优化（Proximal Policy Optimization, PPO）算法在代码生成模型上的微调应用：

- **优势函数估计**：使用广义优势估计（GAE）来平衡偏差与方差
- **正则化约束**：引入 KL 散度惩罚项，防止模型过度偏离原始语言模型的语义分布，避免"乱码刷分"现象

### 2.4 实验验证与性能评估

在标准代码生成数据集（MBPP、HumanEval）上进行对比实验：

- 对比 SFT（监督微调）模型与 RL 微调模型在 Pass@1 指标上的差异
- 分析不同奖励权重设置对模型收敛速度和最终性能的影响

---

## 三、技术路线

```
┌─────────────────────────────────────────────────────────────────────────┐
│                           技术路线总览                                    │
├─────────────────────────────────────────────────────────────────────────┤
│  数据准备 → SFT热启动 → RL闭环训练 → 评估优化 → 论文撰写                    │
└─────────────────────────────────────────────────────────────────────────┘
```

### 3.1 数据准备与环境搭建

1. 收集整理 MBPP、HumanEval 等代码数据集
2. 构建标准化的 `(Prompt, Canonical Solution, Test Cases)` 训练样本
3. 基于 Docker 和 FastAPI 封装强化学习环境类（SandboxEnv）
4. 实现 `step()` 和 `reset()` 接口，打通"模型生成-沙箱执行-奖励返回"的链路

### 3.2 监督微调（SFT）热启动

1. 选择开源代码模型（如 DeepSeek-Coder-1.3B）作为基座
2. 使用标准代码数据集进行有监督微调
3. 使模型初步具备遵循指令生成代码的能力
4. 作为强化学习的"策略网络"初始化权重

### 3.3 强化学习闭环训练（PPO Loop）

```
┌──────────────┐    ┌──────────────┐    ┌──────────────┐    ┌──────────────┐
│   采样        │ → │   评估        │ → │   反馈        │ → │   更新        │
│  (Rollout)   │    │ (Evaluation) │    │  (Feedback)  │    │  (Update)    │
└──────────────┘    └──────────────┘    └──────────────┘    └──────────────┘
       ↑                                                            │
       └────────────────────────────────────────────────────────────┘
```

- **采样（Rollout）**：策略网络根据 Prompt 生成多条代码样本
- **评估（Evaluation）**：将代码送入 Docker 沙箱执行，获取执行结果（Pass/Fail/Error）及性能指标
- **反馈（Feedback）**：计算综合奖励值 $R$，并结合 Critic 网络计算优势值 $A$
- **更新（Update）**：利用 PPO Loss 更新策略网络参数，最大化期望回报，同时约束 KL 散度

### 3.4 结果评估与论文撰写

1. 在测试集上评估模型的一次通过率（Pass@1）
2. 分析实验数据，绘制训练收敛曲线
3. 撰写毕业论文

---

## 四、时间规划

| 阶段 | 时间周期 | 主要任务 | 交付物 |
|:---:|:---:|:---|:---|
| **第一阶段** | 2025.12.22 - 2026.01.31 | 环境搭建与基座模型微调 | SandboxEnv 类、SFT 模型 |
| **第二阶段** | 2026.02.01 - 2026.03.29 | 强化学习算法实现与初步实验 | PPO 训练代码、Baseline 数据 |
| **第三阶段** | 2026.03.30 - 2026.04.05 | 算法优化与中期检查 | 中期报告、消融实验结果 |

### 第一阶段详细任务（2025.12.22 - 2026.01.31）

- [ ] **环境开发**：基于 Docker 和 FastAPI 封装强化学习环境类（SandboxEnv），实现代码提交到奖励返回的自动化闭环
- [ ] **数据清洗**：整理 MBPP 和 HumanEval 数据集，构建标准化的 `(Prompt, Code, TestCases)` 训练样本
- [ ] **SFT 热启动**：选择 DeepSeek-Coder 或类似基座模型，完成监督微调，获得具备初步代码能力的策略网络

### 第二阶段详细任务（2026.02.01 - 2026.03.29）

- [ ] **算法实现**：基于 PPO 算法框架，编写 Actor-Critic 网络训练代码，集成 GAE 优势估算与 KL 散度惩罚项
- [ ] **联调测试**：打通"模型生成-沙箱执行-梯度更新"的全流程，解决训练初期可能遇到的不收敛或环境崩溃问题
- [ ] **初步数据**：运行 Baseline 实验，收集 SFT 模型与 RL 模型的初步对比数据

### 第三阶段详细任务（2026.03.30 - 2026.04.05）

- [ ] **中期成果汇总**：系统梳理前一阶段的实验结果，撰写中期检查报告
- [ ] **消融实验（Ablation Study）**：定量评估不同模块（KL 散度约束、GAE 优势函数估算）对最终性能的贡献度
- [ ] **案例分析**：深入分析模型生成的典型错误案例（Bad Cases），验证算法设计的科学性与鲁棒性

---

## 五、关键技术栈

| 类别 | 技术/工具 |
|:---:|:---|
| 基座模型 | DeepSeek-Coder / Qwen-Coder |
| 沙箱环境 | Docker + FastAPI |
| RL 算法 | PPO + GAE |
| 数据集 | MBPP、HumanEval |
| 评估指标 | Pass@1 |

---

## 六、预期成果

1. **工程交付**：完整的 RL 代码生成训练框架（SandboxEnv + PPO Training Loop）
2. **实验数据**：SFT vs RL 模型的性能对比数据、消融实验结果
3. **学术成果**：毕业论文
